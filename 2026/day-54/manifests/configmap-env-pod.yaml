apiVersion: v1
kind: Pod
metadata:
  name: env-test-pod
spec:
  containers:
    - name: busybox
      image: busybox:1.36
      command:
        ["/bin/sh", "-c", "printenv APP_ENV APP_DEBUG APP_PORT; sleep 3600"]
      envFrom:
        - configMapRef:
            name: app-config
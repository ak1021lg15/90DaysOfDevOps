apiVersion: v1
kind: Pod
metadata:
  name: resources-demo
  labels:
    app: resources-demo

spec:
  containers:
    - name: nginx
      image: nginx:alpine

      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"

        limits:
          cpu: "250m"
          memory: "256Mi"